apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# platform-backend-secrets (GITOPS_REPO_URL with creds) is created imperatively
# by bootstrap.sh so no credentials live in git.
resources:
  - namespace.yaml
  - backend.yaml
  - frontend.yaml
  - ingress.yaml
